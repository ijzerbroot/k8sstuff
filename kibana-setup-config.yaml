apiVersion: v1
kind: ConfigMap
metadata:
  name: kibana-setupsh
  namespace: default
  labels:
    k8s-app: kibana
    kubernetes.io/cluster-service: "false"
data:
  kibana-setupsh: |-
    #!/bin/bash
    # Will have to be done otherwise. requires container restart...
    #cp /etc/resolv.conf /tmp && cat /tmp/resolv.conf | sed 's/nameserver /nameserver 8.8.8.8 /' > /etc/resolv.conf
    #/usr/share/kibana/bin/kibana-plugin install #https://github.com/sivasamyk/logtrail/releases/download/v0.1.23/logtrail-6.1.0-0.1.23.zip
    #cp /conf/logtrailconf /usr/share/kibana/plugins/logtrail/logtrail.json
    /usr/local/bin/kibana-docker
